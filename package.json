{
  "name": "landing",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently --kill-others \"./scripts/build-on-change.sh dev\" \"sleep 1 && live-server --watch=. --entry-file=./index.html --host=0.0.0.0 --port=2222 --wait=500 --no-css-inject dev\"",
    "build": "./scripts/build.sh build"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.0.1",
    "live-server": "^1.2.2",
    "tailwindcss": "^3.4.14"
  }
}
